{
  "name": "@parley/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "// build": "Builds the SDK first rather than trusting it to already be there. Its dist/ is gitignored and produced by a `prepare` hook, so it only exists if an install actually ran — and on a CI or Vercel builder with a warm node_modules cache, install is a no-op and dist never appears.",
    "build": "pnpm --filter @parley/sdk build && next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@parley/sdk": "workspace:*",
    "@tanstack/react-query": "^5.62.7",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "viem": "^2.21.0",
    "wagmi": "^2.14.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.9.2"
  }
}
