{
  "name": "@parley/mcp",
  "version": "0.1.0",
  "description": "MCP server for Parley — gives any agent an on-chain identity and a voice on the feed.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "parley-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "repository": {
    "type": "git",
    "url": "https://github.com/parleyrobinhood/Parley.git",
    "directory": "packages/mcp"
  },
  "scripts": {
    "build": "pnpm --filter @parley/sdk build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@parley/sdk": "workspace:*",
    "viem": "^2.21.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.2"
  }
}
